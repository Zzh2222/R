当你在命令行中运行 pip 命令时，系统会按照PATH环境变量中的顺序查找 pip，并使用找到的第一个版本。因此，你实际使用的将是PATH环境变量中列出的第一个 pip 的路径。  
  
如果你想要使用特定版本的 pip，你可以通过指定完整的路径来运行它，例如 D:\software\python3.9\Scripts\pip install package_name。  
  
同时，如果你在使用Python的虚拟环境（如venv或conda），那么 pip 命令将会使用该虚拟环境中的版本，这可以避免不同版本之间的冲突。

#环境变量