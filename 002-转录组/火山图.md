```

library(DESeq2)
#这里的txt文件是featureCounts产生的count文件，不是标准化后的fpkm文件
counts<-read.table("count.txt",sep = '\t',header = T,row.names = 1)
cts<-counts[,6:11]
cts<-cts[rowSums(cts) !=0,]
condition <- factor(c("t=8","t=8","t=24","t=24","t=0","t=0"),levels = c('t=0','t=8','t=24'))
colData <- data.frame(row.names=colnames(cts),condition)
dds <-DESeqDataSetFromMatrix(countData = cts,colData = colData, design =~ condition)

dds <- DESeq(dds)
#注意，需将treat在前，control在后，意为treat相较于control中哪些基因上调/下调
res_t0_vs_t8 <- results(dds,contrast = c('condition','t=8','t=0'))
res_t0_vs_t8_2 <- res_t0_vs_t8[order(res_t0_vs_t8$padj),]
res_t0_vs_t8_2$significant <- 'unchange'
res_t0_vs_t8_2$significant[res_t0_vs_t8_2$padj <= 0.01 & 
                             res_t0_vs_t8_2$log2FoldChange >= 1] <- 'upregulated'
res_t0_vs_t8_2$significant[res_t0_vs_t8_2$padj <= 0.01 & res_t0_vs_t8_2$log2FoldChange 
                           <= -1] <- 'downregulated'
#将结果保存为文件
write.table(res_t0_vs_t8_2,
            file ='diffff_res_t0_vs_t8.txt',
            quote = F,
            sep = '\t',
            row.names = T,
            col.names = T)

#将差异基因筛选出来并保存为文件
#读取文件
data<-read.table("diff_res_t0_vs_t8.txt",sep = '\t',header = T,row.names = 1)
# 筛选满足条件的行，忽略NA值
filtered_data <- data[abs(data$log2FoldChange) >= 1 & data$padj <= 0.01 & !is.na(data$log2FoldChange) & !is.na(data$padj), ]

# 打印筛选结果
write.csv(filtered_data,file = 'filtergene0~8.csv',row.names = T,）
#
##
library(pheatmap) 
library(ggplot2)
t0_vs_t8 <- as.data.frame(res_t0_vs_t8_2)
ggplot(t0_vs_t8) +
  geom_point(aes(x=log2FoldChange,y=-log10(padj),color=significant))+
  scale_color_manual(values = c('upregulated' = 'red','downregulated' = 'blue' ,'unchange' = 
                                  'grey'))+
  labs(x='log2FC',y='-log10Padj')+
  geom_hline(yintercept = -log10(0.01),linetype = 4)+
  geom_vline(xintercept = c(-1,1),linetype = 4)+
  xlim(-5,5)+
  ylim(0,250) +
  theme(plot.title = element_text(vjust = 0.5,hjust = 0.5),legend.title = element_blank(),
        legend.text = element_text(),
        plot.background = element_rect(fill = 'transparent',colour = 'black'),
        panel.background = element_rect(fill = 'transparent',colour = 'black'))
```

打印代码awk '{print"htseq-count -f bam -s no -r pos -t exon -i gene_id "$1".bam 111.gtf > ../counts/"$1".txt "}'  11.txt >22.sh


批量删除和修改文件中的信息sed -i 's/.bam//g' 11.txt

 samtools fixmate -O bam  *.bam

awk '{print"samtools fixmate -O bam" $1 " *.bam"}'  11.txt >22.sh

awk '{print"featureCounts -a 111.gtf -o "$1".txt  "$1".bam"}'  11.txt >22.sh


sudo umount /mnt/f

sudo mount -t drvfs F: /mnt/f

awk  '{print "hisat2 -x lala -p 20 -1 ../fastqsh/”$1”_1.fastq -2 ../fastqsh/"$1”_2.fastq -S ../sam1/”$1”.sam”}’  33.txt

[[火山图 1]]

